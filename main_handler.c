#include "main_handler.h"
#include "input_handler.h"

// Returns: 0 --- Failed
//          1 --- Success
int handle(int input) {
  int output = parse_key(input);

  // Handle error outputs

  return output;
}
